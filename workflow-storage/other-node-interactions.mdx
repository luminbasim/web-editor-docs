---
title: Other Node Types That Can Write Variables to Workflow Storage
sidebarTitle: Other node interactions
---

There are node types other than `Execute Code` that can write variables to workflow storage (for example the `Get Text` node will read the text of a DOM element and write the value to the specified `Variable name`). We will explicitly say when a given node type writes a value into the storage.
